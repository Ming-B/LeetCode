#include <iostream>
#include <vector>

int carFleet(int target, std::vector<int>& position, std::vector<int>& speed){
    //n cars going the same destination along a one-lane road. 
    //destination is target miles away

    //two integer arrays position and speed, both length n
    //position[i] is the position of the ith car and speed[i] is the speed of the [ith] car

    //car can never pass another car ahead of it, but it can catch up to it and drive
    //bumper to buymper at the same speed
    //faster car will slow down to match the slower car's speed.
    //distance between the two can be ignored

    //car fleet is non empty set of cars driving at the same position and same speed
    //single car is also a car fleet

    //if a car catches up to c ar fleet right at the destination point, it will also be considered as one car fleet

    //return the number of car fleets that will arrive at the destination

    


}